# Client Environment Variables (example)
# Rename this file to .env and fill values

# API base URL (server should be running on this origin)
VITE_API_URL=http://localhost:4444/api

# Google Identity Services Client ID (for One Tap / button)
VITE_GOOGLE_CLIENT_ID=your_google_client_id

# Mapbox public token for maps (frontend usage)
VITE_MAPBOX_TOKEN=your_mapbox_public_token
