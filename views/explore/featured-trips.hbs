<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Featured trips</title>
    <link rel="stylesheet" href="/styles/components/style.css">
    <link rel="stylesheet" href="/styles/components/filter_sort_bar.css">
    <link rel="stylesheet" href="/styles/explore/explore-trips.css">
    <link rel="stylesheet" href="/styles/explore/explore-all-trips.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
</head>
<body>
    {{>navbar}}

    <main>
        <div class="explore-section">
        <a href="/explore#featured-trips" class="btn-back-fixed">←</a>

            <div class="section-heading">
                <h2 class="section-title">✨ Featured Trips</h2>
            </div>

            {{>filter_sort_bar}}

            <div class="trip-card-grid">
                {{#each featuredTrips}}
                    <a href="/explore/trip/{{this._id}}" class="trip-card-modern">
                        <div class="trip-card-modern-img" style="background-image: url('{{this.imageUrl}}');"></div>
                        <div class="trip-card-modern-content">
                        <h2 class="trip-modern-title">{{this.tripName}}</h2>
                        <span class="category-badge-explore">{{this.category}}</span>
                        <p class="trip-modern-destination">📍 {{this.mainDestination}}</p>
                        <p class="trip-modern-dates">🗓️ {{calculateTripDuration this.startDate this.endDate}}</p>
                        {{#if this.description}}
                            <p class="trip-modern-description">{{this.description}}</p>
                        {{/if}}
                        </div>
                    </a>
                {{/each}}
            </div>
        </div>
    </main>

    {{>footer}}
</body>
</html>
