<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{user.name}}'s Profile Settings</title>
    <link rel="stylesheet" href="/styles/components/style.css">
    <link rel="stylesheet" href="/styles/profile.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
</head>
<body>
    {{>navbar}}

    <main class="profile-page">
        <div class="profile-container">
            <div class="profile-card glass">
                <form method="POST" action="/profile/settings" class="profile-form" enctype="multipart/form-data">
                    <h2>Edit Your Profile</h2>

                    {{#if msg}}
                    <p class="form-msg">{{msg}}</p>
                    {{/if}}

                    <div class="form-group">
                    <label>Name</label>
                    <input type="text" name="name" value="{{user.name}}" required>
                    </div>

                    <div class="form-group">
                    <label>Username</label>
                    <input type="text" name="username" value="{{user.username}}" required>
                    </div>

                    <div class="form-group">
                    <label>Email</label>
                    <input type="email" name="email" value="{{user.email}}" required>
                    </div>

                    <div class="form-group">
                    <label>Bio <small>(optional, 300 chars max)</small></label>
                    <textarea name="bio" maxlength="300">{{user.bio}}</textarea>
                    </div>

                    <div class="form-group">
                    <label>Profile Picture</label>
                    <input type="file" name="profilePic" accept="image/*">
                    </div>

                    <div class="form-group">
                    <label>New Password <small>(leave blank to keep current)</small></label>
                    <input type="password" name="newPassword" placeholder="Enter new password">
                    </div>

                    <div class="form-group checkbox-group">
                    <label>Public Profile</label>
                    <input type="checkbox" name="isPublic" value="true" {{#if user.isPublic}}checked{{/if}}>
                    </div>

                    <div class="profile-actions profile-settings-btns">
                    <button type="submit" class="btn-edit btn-save">Save Changes</button>
                    <a href="/profile" class="btn-logout">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </main>



    {{>footer}}
</body>
</html>
