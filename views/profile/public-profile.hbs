<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{profileUser.name}} (@{{profileUser.username}}) | Nomadly</title>
    <link rel="stylesheet" href="/styles/components/style.css">
    <link rel="stylesheet" href="/styles/profile.css">
    <link rel="stylesheet" href="/styles/trip.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
</head>
<body>
    {{>navbar}}

    <main class="profile-page">
        <a href="javascript:history.back()" class="btn-back-fixed">←</a>
        <div class="profile-container">
            <div class="profile-card glass">
                <div class="profile-image">
                    <img src="{{profileUser.profilePic}}" alt="Profile Picture" width="150" height="150">
                </div>
                
                <div class="profile-info">
                    <h2>{{profileUser.name}}</h2>
                    <p>@{{profileUser.username}}</p>
                    {{#if profileUser.bio}}
                        <p class="profile-bio">{{profileUser.bio}}</p>
                    {{/if}}
                </div>
            </div>


            <section class="profile-trips-section glass">
                <div class="profile-trips-header">
                    <h3>{{profileUser.name}}'s Recent Trips</h3>
                </div>

                {{#if trips.length}}
                    <div class="trip-card-mini-grid">
                    {{#each trips as |t|}}
                        <a href="/explore/trip/{{t._id}}" class="trip-card">
                            <div class="trip-card-img" style="background-image: url('{{t.imageUrl}}');">
                                <div class="trip-card-overlay">
                                <h1 class="trip-title">{{t.tripName}}</h1>
                                <div class="card-overlay-bottom">
                                    <p class="trip-destination">{{t.mainDestination}}</p>
                                    <span class="category-badge">{{t.category}}</span>
                                </div>
                                </div>
                            </div>
                        </a>
                    {{/each}}
                    </div>
                {{else}}
                    <p class="empty-state">{{profileUser.name}} hasn't shared any public trips yet.</p>
                {{/if}}
            </section>
        </div>
    </main>

    {{>footer}}
</body>
</html>
